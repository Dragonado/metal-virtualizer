version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # Single-author project with one wire version for now. Re-enable these two
    # if the contract ever needs multiple concurrent versions.
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE
